# Define two variables x and j with values of 10 and 20, respectively
x = 10
j = 20

# Define a function called addAll that returns the sum of four variables
def addAll():
    # Define two variables y and k with values of 4 and 30, respectively
    y = 4
    k = 30
    # Return the sum of x, y, k, and j
    return x + y + k + j

# Call the addAll function and print the result
print(addAll())
