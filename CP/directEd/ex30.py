#Python: Library

# Define the hello function that takes a name as a parameter
def hello(name):
    # Print a greeting message with the name using formatted string
    print(f"Hello {name}")

# Define the goodbye function that takes a name as a parameter
def goodbye(name):
    # Print a farewell message with the name using formatted string
    print(f"Good Bye{name}")
